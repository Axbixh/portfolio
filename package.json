{
  "name": "observatory",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run build --silent && concurrently -n web,api -c yellow,cyan \"npm run dev -w frontend\" \"npm run dev:functions -w frontend\"",
    "build": "npm run build -w frontend",
    "preview": "npm run preview -w frontend",
    "deploy": "npm run build && npm run deploy -w frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
